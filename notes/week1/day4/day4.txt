crontab 
crontab -r removes all crontabs if filename is not mentioned 
crontab -l  list all crontabs 
crontab -e  edit all crontab 
crontab filename --> creates crontab 
for crontab everyseceo sleep 1 
for date touch "$date".txt 



XXXXX --> minute hour date day month 
 
--------------http server with python----
process status ps

background --> bg
foreground --> fg
kill        --> kills a programm
ip address --> ip addr show 

 python -m  SimpleHTTPServer portnumber 
python -m pushd echo "frr" ~/Desktop/vfvv popd popd
 pushd path; python -m SimpleHTTPServer 80 ;popd

--------------Git--------------------------
git -> version control system
                                          before  1) empty repo --->
git init --> initiates a empty repository        2) init a repo --->working directory  
git add. -->tracks the files                      3) tracks a file
git commit-m "filename" -->                         4) makes a commit and makes a local repo
git push origin master 
 
--->everytime we make a change to a file we have to "git add ." again 
--->git clone urlofclone wil clone the repository 
   for cloning create a folder

   

